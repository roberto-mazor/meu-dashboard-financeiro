{
  "name": "fullstack-dashboard",
  "version": "1.0.0",
  "devDependencies": {
    "concurrently": "^8.2.2"  <-- O 'concurrently' instalado
  },
  "scripts": {
    "start-frontend": "npm run dev --prefix frontend",
    "start-backend": "npm run dev --prefix backend",
    "dev": "concurrently \"npm run start-backend\" \"npm run start-frontend\""  <-- O script unificado
    
    // Você pode ter outros scripts de deploy aqui também
  }
}